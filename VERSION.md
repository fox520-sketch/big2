# 版本說明

## v0.6.4 - 2026-06-03
### 新增
- 新增排行榜區塊：顯示目前房間累計總分、勝場與局數。
- 新增房間列表 / 最近房號：可讀取 Firestore 最近房間，也會在本機記錄最近加入過的房號。
- 新增細部音效設定：可分別開關選牌、出牌、Pass、勝利、錯誤提示音，並調整主音量。
- 新增動畫設定：可關閉動畫效果，降低手機閃動與效能負擔。
- 新增手機橫向提示：手機直向時提示可轉橫向或左右滑動手牌。
- 新增規則說明頁：內建基本玩法、牌型、Pass 與計分說明。

### 強化
- 保留 v0.6.3 免 Cloud Functions 架構，不需要 Blaze 付費方案。
- 保留 Firebase 設定檢查面板與多人同步偵錯面板。
- 房間列表讀取失敗時會保留最近房號功能，不影響手動輸入房號加入。

### 測試
- 規則、AI、多人狀態、設定計分、主題對比測試通過。

---

## v0.6.2 - 2026-06-02
### 修正
- 強化多人回合同步檢查：出牌 / Pass 會比對 gameId、目前回合與 revision，避免舊畫面或重複點擊送出過期動作。
- 新增出牌 / Pass 防連點：Firebase transaction 完成前鎖定操作按鈕，避免手機連點造成重複送出。
- 強化下一局穩定性：下一局會重新建立 gameId、清空上一局 lastPlay / passCount / winner / results，並保留累計總分。
- 強化斷線重連：已離線玩家若下一局尚未回來，會保留名稱並由 AI 接管；同 UID 回來可取回座位。
- 新增多人同步偵錯面板：顯示版本、房號、我的座位、房主座位、目前回合、revision、lastActionId、gameId 與最後同步時間。
- 保留 v0.6.1 的選牌穩定修正：一般 Firebase 心跳不會清空已選牌。

### 測試
- 規則、AI、多人狀態、設定計分、主題對比測試通過。
- 新增語法檢查與壓縮檔完整性檢查。


## v0.5.0 - 2026-06-02

### 新增
- 新增多人連續對戰流程：一局結束後，房主可按「下一局 / 重新洗牌」。
- 新增同一房間累計總分、勝場與累計總排名。
- 新增玩家離線偵測，真人玩家逾時未回應時會標示離線。
- 新增離線 AI 接管：進行中的牌局若真人玩家離線，該座位暫時改由 AI 出牌。
- 新增房主轉移：房主離線時，會轉移給下一位已連線真人玩家。
- 新增座位卡顯示離線、AI 接管、總分與勝場。
- 新增斷線重連恢復：同一瀏覽器回到房間後，可重新取回原真人座位。

### 修正
- 修正多人結算後無法延續原房間繼續下一局的問題。
- 修正多人累計分數容易被同一局重複計算的問題。
- 修正房主離線後 AI 補位可能停止出牌的問題。

### 注意
- v0.5.0 仍是朋友休閒測試版，正式防作弊版建議下一階段改用 Cloud Functions 洗牌、發牌與驗證出牌。
- 更新此版後，請同步把新版 `firestore.rules` 貼到 Firebase Console 並 Publish。

---

## v0.4.0 - 2026-06-02

### 新增
- 新增房主「開始多人遊戲」按鈕。
- 新增多人同步洗牌與發牌。
- 新增多人出牌同步。
- 新增多人 Pass 同步。
- 新增多人回合同步、上一手同步、領出權同步。
- 新增多人模式每位玩家依自己的座位顯示「我的手牌」。
- 新增座位卡顯示真人、AI、房主、你、手牌張數。
- 新增 AI 補位接管：輪到 AI 時，由房主瀏覽器自動執行 AI 出牌。
- 新增多人一局結束後同步分數與排名。
- 更新 `firestore.rules`，允許 v0.4.0 的 `game` 牌局狀態同步。
- 更新 `docs/FIREBASE_SETUP.md`，加入多人測試流程。
- 新增 `scripts/test-multiplayer-state.js` 多人狀態測試。

### 修正
- 修正 v0.3.0 只有房間大廳、真人連線後仍不能一起玩的問題。
- 修正多人模式下操作按鈕只會在輪到自己的座位時啟用。
- 修正多人模式下單人 AI 自動排程不應繼續執行的問題。

### 尚未完成
- Cloud Functions 正式防作弊版。
- 玩家離線後完整 AI 代打接管。
- 斷線重連後更細緻的座位恢復與踢除機制。

---

## v0.3.0 - 2026-06-02

### 新增
- 新增 Firebase 多人房間區塊。
- 新增 `src/firebase-config.js`，可貼上 Firebase Web App 設定。
- 新增 `src/firebase-room.js`，處理 Firebase 初始化、匿名登入、建立房間、加入房間、監聽房間、離開房間、AI 補位。
- 新增建立房間功能，產生 6 碼房號。
- 新增加入房間功能。
- 新增邀請連結：`?room=房號&join=1`。
- 新增邀請連結自動加入：Firebase 匿名登入完成後會真正執行 `joinRoom()`。
- 新增 QR Code 顯示，可用手機掃描加入房間。
- 新增房間座位列表，顯示房主、真人、AI、連線狀態。
- 新增房主「補 AI 空位」按鈕。
- 新增 `firestore.rules` 休閒測試版安全規則。
- 新增 `docs/FIREBASE_SETUP.md` Firebase 設定教學。

### 修正
- GitHub Pages 邀請連結會保留目前路徑，不會固定寫死網址。
- 房號輸入會自動轉大寫並移除非英數字元。
- 未填 Firebase 設定時，單人遊戲仍可正常執行，不會因 Firebase 失敗而整頁無法載入。

### 尚未完成
- 多人同步出牌。
- 多人局洗牌與發牌同步。
- Cloud Functions 防作弊驗證。
- 玩家離線後由 AI 接管完整出牌流程。

---

## v0.2.0 - 2026-06-02

### 新增
- AI 難度 1～20 級。
- 完整 13 種主題風格。
- UI 對比檢查清單。
- AI 測試腳本。

---

## v0.1.1 - 2026-06-02

### 修正
- 修正深色模式下黑桃、梅花看不清楚的問題。
- 強化所有主題牌面紅黑花色對比。

---

## v0.1.0 - 2026-06-02

### 新增
- 台灣大老二規則引擎。
- 單人模式：玩家 + 3 家簡易 AI。
- 出牌 / Pass / 輪替邏輯。
- 遊戲結束後計分與排名。
