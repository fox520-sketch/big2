# 版本說明

## v0.8.4 - 2026-06-06
### 正式發布候選版（RC）
- 修正房主離線後的跨用戶房主轉移：房主逾時時，任何仍在房內的真人可透過 transaction 觸發確定性轉移。
- 加入同裝置多分頁 AI 回合租約，降低兩個房主分頁同時控制 AI；舊 revision 仍會被 Firestore transaction 拒絕。
- PWA 保留最近一版靜態快取，新增回復上一版、恢復最新版與回復狀態診斷。
- 強化 Firestore Rules：非房內成員不可修改既有房間；新玩家只能在等待／結束狀態加入單一座位。
- 實機驗收項目增加 Rules 安全檢查與回復演練。
- 新增 `RC_ACCEPTANCE_CHECKLIST.md`、`KNOWN_LIMITATIONS.md`、`ROLLBACK_GUIDE.md`、`FIREBASE_USAGE_GUIDE.md`、`FIRESTORE_RULES_SECURITY_REVIEW.md`。
- 維持免 Cloud Functions、不需要 Blaze。

## v0.8.3 - 2026-06-06
### 正式上線實機驗收中心
- 新增 Android、iPhone／iPad、桌機 PWA、2～4 位真人、斷線重連、房主轉移、下一局、手機 UI 與 Firebase 讀寫等 12 項實機驗收清單。
- 驗收進度保存在本機，可執行快速技術檢查、複製摘要與重設狀態。
- 自動檢查只標記可由瀏覽器判斷的 PWA 與 listener 健康；真實裝置與多人對戰仍需手動確認。

### 錯誤紀錄中心
- 自動保留最近 20 筆 JavaScript error、unhandled rejection 與多人操作錯誤。
- 每筆包含功能、錯誤代碼、房號、gameId、座位、目前回合、網路、頁面狀態與最近操作。
- 支援複製、下載及清除；資料只儲存在本機，不會自動上傳。

### Firebase 監聽恢復與效能驗證
- 新增 listener 最後快照時間、監聽錯誤與恢復次數統計。
- 網路恢復或 App 回到前景時，若 listener 發生錯誤、遺失或超過 150 秒未收到快照，會自動解除並重建。
- 錯誤中心顯示 listener 啟停差額、快照、心跳、文件讀取與寫入數量。

### 文件與測試
- 新增 `docs/REAL_DEVICE_ACCEPTANCE_V083.md`、`docs/ERROR_LOG_CENTER.md`。
- 新增 `scripts/test-v083-acceptance.js`、`scripts/test-error-center.js`。
- 維持免 Cloud Functions、不需要 Blaze，未新增 Firestore 欄位。

---
## v0.8.2 - 2026-06-06
### PWA 一鍵修復與更新工具
- 新增「一鍵修復 PWA」，會檢查新版、清除 Big2 舊快取、重建離線 App Shell 並重新載入。
- 新增立即套用新版、重新下載離線檔案、清除舊版快取與重新載入遊戲等獨立操作。
- Service Worker 新增 `REPAIR_PWA`、`CLEAR_OLD_CACHES` 訊息流程。

### 問題回報與診斷匯出
- 原偵錯資訊升級為完整問題回報，可複製或下載 `.txt` 診斷檔。
- 診斷內容包含 PWA／Service Worker 版本、Cache Storage、儲存空間、顯示模式、裝置與視窗資訊。
- 加入 Firebase listener、快照、心跳、查詢快取、文件讀取與寫入等本機計數。
- 收集最近 20 筆 JavaScript error 與 unhandled rejection，便於追查實機問題。

### Firebase 效能與過期資料保護
- 最近房間列表加入 15 秒查詢快取，避免連續點擊重複讀取 Firestore。
- 前景心跳維持 25 秒；背景心跳降為 75 秒，離線判定放寬為 180 秒，回到前景立即恢復同步。
- 離房與更換 listener 時確實清除監聽及計時器。
- 過期房間清理提供詳細結果，安全略過目前房間、進行中與近期房間。

### 驗收文件與測試
- 新增 `docs/FINAL_ACCEPTANCE_CHECKLIST.md`、`docs/TROUBLESHOOTING.md`、`docs/DIAGNOSTIC_REPORT.md`。
- 新增 PWA 修復／診斷、Firebase 效能與正式驗收自動測試。
- 維持免 Cloud Functions、不需要 Blaze，未新增 Firestore 文件欄位。

---
## v0.8.1 - 2026-06-06
### PWA 安裝、路徑與更新修正
- Service Worker 使用 `self.registration.scope` 建立 GitHub Pages 子目錄網址，避免 `/big2/` 部署路徑錯誤。
- 修正所有導覽回應都寫入 `index.html` 快取的問題；隱私頁與首頁現在各自快取。
- JavaScript、CSS、manifest 改為網路優先並保留版本 query，不再用 `ignoreSearch` 混用新舊資源。
- 加入 navigation preload、網路逾時與離線 fallback。
- 新增「檢查更新」、「更新離線檔案」、快取容量與版本狀態。
- 更新檢查會在定期、恢復網路、頁面回到前景與 PWA 再次開啟時執行。

### Firebase 多人恢復
- Firebase SDK 動態 import 失敗時會清除 rejected Promise，恢復網路後可重新載入。
- 重新連線時呼叫 Firestore `enableNetwork()`。
- 房間 listener 因錯誤中止後，會使用原 callback 重新掛上監聽。
- 新增 `pageshow` 恢復處理，改善從背景、BFCache 或鎖定螢幕返回時的同步。

### 手機、SEO 與無障礙
- 偵測手機虛擬鍵盤，開啟時隱藏非必要提示，避免輸入欄位被擠壓。
- 新增 Firebase CDN preconnect、Open Graph secure URL 與圖片 MIME 類型。
- 新增跳至主要內容連結、強化鍵盤焦點、200% 文字縮放、高對比與 forced-colors 支援。
- 首次使用導覽改用穩定旗標，已完成的使用者不會在每次更新後重複看到。

### 文件與測試
- 新增 `docs/PWA_ONLINE_VERIFICATION.md`。
- 更新 `docs/PWA_RELEASE_CHECKLIST.md`。
- 新增 `scripts/test-pwa-online-verification.js`，檢查 scope、更新策略、Firebase 重連、手機鍵盤與無障礙。
- 維持免 Cloud Functions、不需要 Blaze，本版未新增 Firestore 文件欄位。

---
## v0.7.5 - 2026-06-05
### 手機實戰回歸與效能穩定
- 使用 `visualViewport` 與 CSS `--app-height` 同步手機可視高度，降低 Android Chrome、iPhone Safari 網址列伸縮造成的畫面跳動。
- 加入 `viewport-fit=cover` 與 safe-area 留白，避免瀏海、圓角螢幕及底部 Home Indicator 壓住內容。
- 強化手牌水平滑動、最後一張牌尾端安全留白與窄螢幕尺寸；手機直向操作面板改為正常文件流，避免遮擋牌面。
- 手機橫向保留側邊黏附操作，並以動態可視高度限制面板高度。

### 網路與 Firebase 穩定
- 新增網路／同步狀態列，顯示正常、離線、重新連線及恢復狀態。
- 網路恢復或頁面回到前景時，主動恢復 Firebase 心跳與房主離線座位整理。
- 心跳調整為 25 秒、房主整理調整為 30 秒、離線判定放寬為 90 秒，降低手機背景切換誤判與不必要讀寫。
- 同一房間重複監聽時沿用既有 listener；房間快照忽略只有心跳時間改變的更新，避免 UI 重繪與選牌閃動。
- 新增「清理我的過期房間」，可刪除自己建立、超過 24 小時且未進行中的房間。

### 下一局與測試
- 禁止牌局進行中重複洗牌，並以 `expectedGameNo` 阻擋多視窗或舊畫面重複建立下一局。
- 新增 `docs/MOBILE_PERFORMANCE_CHECKLIST.md`。
- 新增 `scripts/test-performance-stability.js`，檢查動態視窗、安全區、手牌滑動、網路重連、監聽去重、過期房間清理與下一局保護。
- 維持免 Cloud Functions，不需要 Blaze。

---
## v0.7.4 - 2026-06-05
### 手機選牌裁切修正
- 增加手機手牌橫向捲動區上方安全留白，選中的牌向上浮起時不再碰到捲動容器頂端。
- 降低手機版選牌上浮位移並保留適度縮放，仍能清楚辨識已選牌，但不會截掉牌面上緣。
- 將手機版選牌勾勾移到牌面內側，避免勾勾與外框超出可視區而被裁切。
- 選中牌提高堆疊層級，左右相鄰牌不會蓋住外框或勾勾。
- 針對窄螢幕與橫向手機再微調上方留白及上浮距離。

### 測試
- 新增 `scripts/test-mobile-selected-card.js`，檢查手機手牌安全留白、選牌位移、勾勾位置與 GitHub Pages 快取版本。
- 規則、AI、多人狀態、計分、對比、發布、線上準備、手機牌桌與精簡操作測試全部保留。

### 架構
- 仍為免 Cloud Functions 版，不需要 Blaze，也不需要 `functions/`。
- 本版未變更 Firestore 欄位與權限規則。

---
## v0.7.3 - 2026-06-05
### 手機操作精簡
- 操作面板預設只顯示目前選牌狀態、出牌與 Pass，降低手機畫面高度。
- 將「推薦出牌」、「最小可出」、「清除選牌」、牌型建議、Pass 提醒設定與 Pass 說明移入可收合的「出牌輔助與 Pass 設定」。
- 出牌輔助預設收合，需要時再展開，不影響原有推薦、最小可出與清除選牌功能。
- 縮小手機操作面板內距、說明區高度與按鈕間距，讓牌桌、手牌與主要操作更集中。
- 保留 v0.7.2 的牌桌緊鄰手牌、玩家資訊橫向滑動與手機底部黏附操作。

### 測試
- 新增 `scripts/test-compact-action-panel.js`，檢查出牌輔助預設為收合結構、主要按鈕位於輔助區之前，並確認 v0.7.3 快取版本參數。
- 規則、AI、多人狀態、計分、對比、發布、線上準備與手機牌桌測試全部保留。

### 架構
- 仍為免 Cloud Functions 版，不需要 Blaze，也不需要 `functions/`。

---
## v0.7.2 - 2026-06-05
### 手機 UI 優化
- 重整牌局 DOM 與 CSS Grid：桌機為「牌桌 + 操作／手牌橫跨下方」，手機為「牌桌 → 手牌 → 操作」。
- 牌桌上一手牌現在直接緊鄰自己的手牌，減少手機上下捲動距離。
- 手機玩家狀態改為橫向滑動卡片；牌局專注模式的座位卡改為雙欄緊湊排列。
- 上一手牌面置中並縮小留白，手牌仍保留橫向滑動與選牌上浮效果。
- 操作面板位於手牌下方並維持底部黏附，讓出牌、Pass 與推薦操作更順手。
- 加入窄螢幕與橫向手機細部調整，避免操作面板遮住最後一張手牌。

### 測試
- 新增 `scripts/test-mobile-table-layout.js`，檢查牌桌、手牌、操作的 DOM 順序與手機版 Grid 配置。
- 規則、AI、多人狀態、計分、對比、發布與線上準備測試全部保留。

### 架構
- 仍為免 Cloud Functions 版，不需要 Blaze，也不需要 `functions/`。

---
## v0.7.1 - 2026-06-04
### 修正
- 強化 GitHub Pages 上線穩定性：CSS 與 JS 加入版本參數，降低瀏覽器快取讀到舊版檔案的機率。
- 強化 Firebase 建立房間與加入房間防呆：建立、加入、補 AI、開始多人遊戲與離開房間皆加入按鈕鎖定，避免手機連點造成重複送出。
- 強化 QR Code 顯示：新增載入狀態、載入失敗提示與備援圖片連結；若 QR 圖片服務暫時失敗，可改用複製邀請連結。
- 強化密碼房加入提示：密碼錯誤時會聚焦到房間密碼欄位，並顯示白話處理方式。
- 強化 Firebase / Rules 錯誤訊息：權限、Rules、網路與房間不存在等狀況會顯示更明確的處理方向。
- 新增「正式上線實測修正」區塊，列出本版針對 GitHub Pages、房間操作、QR Code 與密碼房的修正重點。

### 測試
- 新增 `scripts/test-online-readiness.js`，檢查 v0.7.1 上線實測修正是否存在。
- 規則、規則邊界、AI、多人大廳狀態、設定計分、主題對比與發布檢查全部通過。

### 保留
- 保留 v0.7.0 正式發布候選版的首頁流程、新手教學、房間體驗、防呆與發布檢查清單。
- 仍為免 Cloud Functions 版，不需要 Blaze 付費方案，也不需要 `functions/`。

---
## v0.6.9 - 2026-06-04
### 新增
- 新增 `scripts/test-rule-boundaries.js`，全面檢查單張、對子、三條、五張牌型、起手牌、Pass、結算與累計總分。
- 新增「複製偵錯資訊」按鈕，可一次複製版本、房號、座位、目前回合、上一手、gameId、revision、玩家列表與最近歷史。
- 可出牌掃描新增牌型摘要，例如「目前有 12 組可出牌（單張 6、對子 2、順子 4）」。

### 修正
- 校正並補測台灣常用規則：梅花 3 起手、方塊 3 變體、A2345 變體、順子含 2 變體、五張牌型大小排序。
- 強化壓牌邏輯測試：張數不符、四張牌不能直接出、鐵支需 5 張、五張牌型按順子 < 同花 < 葫蘆 < 鐵支 < 同花順比較。
- 強化 Pass 流程測試：領出者不能 Pass，其他三家 Pass 後回到最後出牌者。
- 強化 AI 出牌策略：高難度更少亂拆 2、A、K、對子與牌型；對手剩 1 張時更積極攔截。
- 修正 AI 在評估剩餘手牌時未套用目前房間規則的問題，現在會依照房間規則判斷 A2345 與順子含 2。
- 強化結算與排行榜檢查，避免同一 gameNo 重複灌入累計總分。

### 保留
- 保留 v0.6.8 的手牌排序、推薦出牌、出牌提示、Pass 提醒與手機選牌優化。
- 仍為免 Cloud Functions 版，不需要 Blaze 付費方案。

---
## v0.6.7 - 2026-06-04
### 新增
- 新增「牌局專注模式」：遊戲進行中自動隱藏規則設定、房間列表、Firebase 檢查、偵錯面板、成就、紀錄、規則說明與防作弊摘要等非必要區塊。
- 新增右上角「顯示設定 / 隱藏設定」按鈕，遊戲中可手動展開或收合被隱藏的功能。
- 多人牌局進行中保留必要區塊：狀態列、房間座位、玩家狀態、上一手、出牌操作與我的手牌。
- 單人遊戲進行中也會進入專注模式，減少手機畫面過長與誤觸。

### 修正
- 遊戲中不再顯示大量不相關設定與說明，讓牌桌區更集中。
- 牌局結束或回到等待房間時，自動恢復顯示設定、排行榜、紀錄與說明區塊。
- 手機版專注模式下縮短房間資訊與座位卡間距，提升出牌操作可視範圍。

### 保留
- 仍為免 Cloud Functions 版，不需要 Blaze 付費方案。
- 保留 v0.6.6 的成就徽章、每日戰績、房間密碼、踢除玩家、重新安排座位與遊戲紀錄。

---
## v0.6.6 - 2026-06-04
### 新增
- 新增成就徽章：首局、首勝、今日 3 局、多人首局、累計 10 局、單局 30 分、累計 5 勝。
- 新增每日戰績：今日局數、勝場、分數、最佳單局與多人局數。
- 強化玩家名稱記憶：暱稱會保存在本機，重新開啟網頁會自動帶入。
- 新增房間密碼：建立房間可設定密碼，加入私人房時需輸入密碼。
- 新增房主管理：房主可踢除玩家；等待中 / 本局結束時可重新安排座位。
- 新增本機遊戲紀錄：保留最近 40 局，記錄時間、房號、排名、分數與勝者。
- 新增更多音效：開局音、加入房間音、成就音、座位調整音，並可分別開關。

### 修正
- 房間列表與最近房號會顯示是否為密碼房。
- Firestore Rules 增加 v0.6.6 的密碼欄位白名單。
- 多人房主操作失敗時會顯示更明確提示。

### 注意
- 房間密碼屬於朋友休閒版保護，密碼雜湊仍儲存在 Firestore 房間文件內，不等同正式後端防作弊。
- 此版仍為免 Cloud Functions 版，不需要 Blaze 付費方案。

---
## v0.6.5 - 2026-06-03
### 修正
- 強化排行榜顯示：加入最後同步時間、上局排名摘要，避免多人房間切換後看不出資料是否已更新。
- 強化最近房號：最近房號會保留真人 / AI 人數資訊，並自動忽略過舊紀錄，避免列表越來越亂。
- 強化房間列表：進行中房間的按鈕改顯示「回房」，提示同裝置玩家可重新取回座位。
- 修正細部音效設定缺少「發牌音」開關的問題，新增發牌音開關與發牌提示音。
- 優化動畫：手牌不再每次畫面重繪都套用發牌動畫，降低手機上選牌或 Firebase 同步時的閃動感。
- 強化多人出牌 / Pass 防連點：加入本機短時間冷卻，避免手機連按造成重複送出或錯誤提示。
- 強化建立房間穩定性：建立房間時會避免極低機率的房號碰撞覆蓋既有房間。
- 補上真正的規則說明頁區塊，方便在遊戲中查看牌型、Pass、計分與排行榜規則。
- 手機版操作優化：房間按鈕、邀請按鈕與房間列表加入按鈕在小螢幕上改為單欄大按鈕。

### 保留
- 仍為免 Cloud Functions 休閒版，不需要 Blaze 付費方案。
- 保留 v0.6.4 的排行榜、房間列表、最近房號、音效動畫設定、手機橫向提示。
- 保留 v0.6.3 的 Firebase 設定檢查面板與多人同步偵錯面板。

### 測試
- 規則、AI、多人狀態、設定計分、主題對比測試通過。
- JavaScript 語法檢查通過。

---
## v0.6.2 - 2026-06-02
### 修正
- 強化多人回合同步檢查：出牌 / Pass 會比對 gameId、目前回合與 revision，避免舊畫面或重複點擊送出過期動作。
- 新增出牌 / Pass 防連點：Firebase transaction 完成前鎖定操作按鈕，避免手機連點造成重複送出。
- 強化下一局穩定性：下一局會重新建立 gameId、清空上一局 lastPlay / passCount / winner / results，並保留累計總分。
- 強化斷線重連：已離線玩家若下一局尚未回來，會保留名稱並由 AI 接管；同 UID 回來可取回座位。
- 新增多人同步偵錯面板：顯示版本、房號、我的座位、房主座位、目前回合、revision、lastActionId、gameId 與最後同步時間。
- 保留 v0.6.1 的選牌穩定修正：一般 Firebase 心跳不會清空已選牌。

### 測試
- 規則、AI、多人狀態、設定計分、主題對比測試通過。
- 新增語法檢查與壓縮檔完整性檢查。


## v0.5.0 - 2026-06-02

### 新增
- 新增多人連續對戰流程：一局結束後，房主可按「下一局 / 重新洗牌」。
- 新增同一房間累計總分、勝場與累計總排名。
- 新增玩家離線偵測，真人玩家逾時未回應時會標示離線。
- 新增離線 AI 接管：進行中的牌局若真人玩家離線，該座位暫時改由 AI 出牌。
- 新增房主轉移：房主離線時，會轉移給下一位已連線真人玩家。
- 新增座位卡顯示離線、AI 接管、總分與勝場。
- 新增斷線重連恢復：同一瀏覽器回到房間後，可重新取回原真人座位。

### 修正
- 修正多人結算後無法延續原房間繼續下一局的問題。
- 修正多人累計分數容易被同一局重複計算的問題。
- 修正房主離線後 AI 補位可能停止出牌的問題。

### 注意
- v0.5.0 仍是朋友休閒測試版，正式防作弊版建議下一階段改用 Cloud Functions 洗牌、發牌與驗證出牌。
- 更新此版後，請同步把新版 `firestore.rules` 貼到 Firebase Console 並 Publish。

---

## v0.4.0 - 2026-06-02

### 新增
- 新增房主「開始多人遊戲」按鈕。
- 新增多人同步洗牌與發牌。
- 新增多人出牌同步。
- 新增多人 Pass 同步。
- 新增多人回合同步、上一手同步、領出權同步。
- 新增多人模式每位玩家依自己的座位顯示「我的手牌」。
- 新增座位卡顯示真人、AI、房主、你、手牌張數。
- 新增 AI 補位接管：輪到 AI 時，由房主瀏覽器自動執行 AI 出牌。
- 新增多人一局結束後同步分數與排名。
- 更新 `firestore.rules`，允許 v0.4.0 的 `game` 牌局狀態同步。
- 更新 `docs/FIREBASE_SETUP.md`，加入多人測試流程。
- 新增 `scripts/test-multiplayer-state.js` 多人狀態測試。

### 修正
- 修正 v0.3.0 只有房間大廳、真人連線後仍不能一起玩的問題。
- 修正多人模式下操作按鈕只會在輪到自己的座位時啟用。
- 修正多人模式下單人 AI 自動排程不應繼續執行的問題。

### 尚未完成
- Cloud Functions 正式防作弊版。
- 玩家離線後完整 AI 代打接管。
- 斷線重連後更細緻的座位恢復與踢除機制。

---

## v0.3.0 - 2026-06-02

### 新增
- 新增 Firebase 多人房間區塊。
- 新增 `src/firebase-config.js`，可貼上 Firebase Web App 設定。
- 新增 `src/firebase-room.js`，處理 Firebase 初始化、匿名登入、建立房間、加入房間、監聽房間、離開房間、AI 補位。
- 新增建立房間功能，產生 6 碼房號。
- 新增加入房間功能。
- 新增邀請連結：`?room=房號&join=1`。
- 新增邀請連結自動加入：Firebase 匿名登入完成後會真正執行 `joinRoom()`。
- 新增 QR Code 顯示，可用手機掃描加入房間。
- 新增房間座位列表，顯示房主、真人、AI、連線狀態。
- 新增房主「補 AI 空位」按鈕。
- 新增 `firestore.rules` 休閒測試版安全規則。
- 新增 `docs/FIREBASE_SETUP.md` Firebase 設定教學。

### 修正
- GitHub Pages 邀請連結會保留目前路徑，不會固定寫死網址。
- 房號輸入會自動轉大寫並移除非英數字元。
- 未填 Firebase 設定時，單人遊戲仍可正常執行，不會因 Firebase 失敗而整頁無法載入。

### 尚未完成
- 多人同步出牌。
- 多人局洗牌與發牌同步。
- Cloud Functions 防作弊驗證。
- 玩家離線後由 AI 接管完整出牌流程。

---

## v0.2.0 - 2026-06-02

### 新增
- AI 難度 1～20 級。
- 完整 13 種主題風格。
- UI 對比檢查清單。
- AI 測試腳本。

---

## v0.1.1 - 2026-06-02

### 修正
- 修正深色模式下黑桃、梅花看不清楚的問題。
- 強化所有主題牌面紅黑花色對比。

---

## v0.1.0 - 2026-06-02

### 新增
- 台灣大老二規則引擎。
- 單人模式：玩家 + 3 家簡易 AI。
- 出牌 / Pass / 輪替邏輯。
- 遊戲結束後計分與排名。
